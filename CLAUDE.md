# 2026 국가데이터 활용대회 — 프로젝트 컨텍스트

> **목표**: 입상  
> **역할**: 팀원 A (데이터 엔지니어 — 데이터 결합·파생변수·Master Table)  
> **분석 주제**: 고령층 경제 이질성 진단 및 지역 맞춤형 자산 정책 제안

---

## 1. 분석 개요

### 핵심 스토리
한국 고령층은 **자산은 부유하나 현금 흐름은 빈곤한** 구조적 모순을 가짐.

- 순자산: 전체 가구 평균의 **1.038배** (2024)
- 부동산 편중: 자산의 **80% 이상**이 부동산
- OECD 노인 빈곤율 **1위** — 집은 있는데 쓸 돈이 없음
- 핵심 발견: **B유형(사각지대)** 전국 약 **468,038가구**  
  → 법적 빈곤층이면서 고자산 보유 → 복지에서 배제 → 자산 유동화만이 해법

### 팀 구성 및 역할 분담

| 팀원 | 역할 | 핵심 산출물 |
|------|------|------------|
| **A (나)** | 데이터 엔지니어 | 데이터 결합, 파생변수 3종, Master Table |
| B | 모델 분석가 | K-Means 군집, XGBoost, SHAP 해석 |
| C | 공간 전략가 | SGIS 격자 매핑, 핫스팟, 정책 제언 |

---

## 2. 데이터 소스 및 실제 경로

### 가계금융복지조사 (Google Drive)
```
/content/drive/MyDrive/Data_Analysis_Competition/관련데이터/가계금융복지조사2023,2024/
  2023_가구마스터_20260512_38026.csv
  2024_가구마스터_20260512_38026.csv
```
- 구분자: `,` / 인코딩: `cp949`
- 컬럼 수: **30개** (R코드 colnames 기준)
- NA값: `*` ~ `**********`, `.`
- **최적화 없이 전체 로드** (요청 사항)

### SGIS 통계등록부 (VDR)
```
/Rdata1/r1_user138/dataset/
  A09_POPULATION/POPULATION_2023.txt     ← 인구통계등록부 (13항목)
  A10_HOUSEHOLD/HOUSEHOLD_2023.txt       ← 가구통계등록부 (14항목)
  A11_HOUSING/HOUSING_2023.txt           ← 주택통계등록부 (10항목)
```
- 구분자: `|` / 메모리 최적화: dtype `category` + `chunksize=200_000`

### 민간 데이터 (VDR, 월별 01~12)
```
P46_CARD_SALES/03.CARD_DOMESTIC_SEL/CARD_DOMESTIC_SEOUL_202401~12.txt   ← 카드소비(서울) 29항목
P46_CARD_SALES/07.CARD_APARTMENT/CARD_APARTMENT_202401~12.txt           ← 아파트단지별소비 29항목
P47_NICE_CREDIT/01.LOAN_ADM/NICE_LOAN_AGE_202401~12.txt                 ← NICE 대출·연체 50항목
P47_NICE_CREDIT/02.INCOM_ADM/NICE_INCOM_AGE_202401~12.txt               ← NICE 소득 20항목
P47_NICE_CREDIT/07.POS_SGG/TN_PD_NICE_POG_SGG_202501.txt               ← 외식업물가 (단일)
P49_NHCARD/05.NH_APARTMENT/NHCRD_APARTMENT_202401~12.txt                ← 농협카드 아파트 29항목
```

---

## 3. 항목명 주의사항 (오타 시 KeyError 발생)

### 가계금융복지조사 — 긴 컬럼명
```python
INC_COL = '처분가능소득(보완)[경상소득(보완)-비소비지출(보완)]'  # 대괄호+소괄호 혼합
RE_COL  = '자산_실물자산_부동산_거주주택금액'
WT_COL  = '가중값'
```

### NICE 대출·연체 — 공백 포함 항목명
```python
'월 연체보유자수 합계'   # 공백 2개 포함
```

### NICE 소득 — 공백 포함 항목명
```python
'직업 구분'          # 공백 1개
'평균 연소득 금액'   # 공백 2개
'중위 연소득 금액'   # 공백 2개
'최저 연소득 금액'   # 공백 2개
'최대 연소득 금액'   # 공백 2개
```

### SGIS JOIN 키
```python
# 가구 ↔ 인구: HOHL_SPIDN (가구주통계목적고유번호)
# 가구 ↔ 주택: LVQT_SN + ADMDST_CLSF_CD (거처일련번호 + 행정구역분류코드)
```

### 카드소비 필터 조건 (문자열 타입 필수)
```python
df['통합카드5세단위연령코드'].isin(['14', '15'])  # 65세 이상 — 반드시 str
df['통합카드가구형태코드'] == '5'                 # 노인가구 — 반드시 str
df['조직구분코드'] == '1'                         # 개인 — 반드시 str
```
> ⚠️ dtype 미지정 시 정수로 읽혀 필터 결과 0행 발생

---

## 4. 핵심 파생변수 3종

### D-Value (실질소비여력지수)
```python
# 카드매출/신고소득 → 소득통계 빈곤 ≠ 실제 소비 수준 입증
D_Value_proxy = (식료품비 + 의료비) / (처분가능소득 + 1e-6)
# D_Value > 1.0 → 자산 기반 소비 (소득 이상 지출)
# 소득 하위20%인데 D_Value > 1.0 → '유형 빈곤' 지역
```

### LTI (유동성함정지수, 0~1)
```python
LTI = 0.35×부동산편중도 + 0.25×금융자산빈곤도 + 0.25×연체리스크 + 0.15×소득여유부족
# LTI > 0.7 → 유동성 함정 → 주택연금 최우선 타겟
등급: 정상(≤0.3) / 주의(≤0.5) / 경고(≤0.7) / 유동성함정(>0.7)
```

### AAI (자산노후도지수)
```python
AAI_proxy = 부동산편중도 × 거주주택금액 / 10_000
# 팀원 C SGIS 핫스팟 분석 연계 — 노후주택에 묶인 자산 규모
```

---

## 5. 고령층 유형 분류 (3중 교차 검증)

```python
# 2026 정부 고시 기준 중위소득 50% (월→연, 만원)
POVERTY_LINE_2026 = {
    1: 1538.5,   # 1인가구
    2: 2517.3,   # 2인가구
    3: 3214.3,   # 3인가구
    4: 3897.0,   # 4인가구
    5: 4546.0,   # 5인가구
    6: 5162.8,   # 6인가구 이상
}

# 분류 기준
B유형 = 정부기준빈곤(중위50%↓) AND 소득분위_내부 ≤ 2 AND 자산분위_내부 ≥ 4
A유형 = 정부기준빈곤            AND 자산분위_내부 ≤ 2
D유형 = 소득분위_내부 > 2       AND 자산분위_내부 ≥ 4
C유형 = 나머지
```

**분류 결과 (가중치 적용 전국 추정)**

| 유형 | 전기고령자(65-74) | 후기고령자(75+) | 합계 |
|------|----------------|----------------|------|
| A유형_구조적취약층 | 481,159 | 780,912 | 1,262,071 |
| B유형_자산소득불일치(사각지대) | **169,070** | **298,968** | **468,038** |
| C유형_기타일반가구 | 1,473,727 | 958,938 | 2,432,665 |
| D유형_여유자산가층 | 1,544,924 | 700,935 | 2,245,859 |

> ⚠️ **중요**: 정부기준빈곤선은 '경상소득' 기준. 수급자 선정 시 '소득인정액(재산 소득환산 포함)'과 다름 — 보고서에 반드시 명시

---

## 6. VDR 반출 규정 (4가지 — 절대 준수)

| 규정 | 내용 | 구현 방법 |
|------|------|-----------|
| **규정 1** | 로우데이터·중간산출물 반출 금지 | 모든 결과 `groupby` 집계 후 저장 |
| **규정 2** | K-Anonymity (건수 < 5 제거) | `apply_k_anonymity(df, count_col, k=5)` |
| **규정 3** | 파일 포맷 제한 (`.py` 반출 불가) | 수치 → `.xlsx`, 시각화 → `.png` (수치 라벨 없음) |
| **규정 4** | inf/NaN 방어 | `replace([np.inf, -np.inf], np.nan).fillna(0)` |

```python
# 핵심 3개 공통 함수
def clean_inf_nan(df, fill_val=0):     # 규정 4
def apply_k_anonymity(df, count_col, k=5):  # 규정 2
def save_xlsx(df, filename):            # 규정 3
```

### 반출 산출물 목록
```
output/excel/
  01_유형별_집계.xlsx            ← 연령그룹 × 유형 × 조사연도
  02_수도권별_유형집계.xlsx      ← 수도권여부 × 연령그룹 × 유형
  03_주택종류별_집계.xlsx        ← 주택종류 × 연령그룹
  04_SGIS_시군구별_주거현황.xlsx ← SGIS 시군구 × 연령그룹 × 거처종류
  05_카드소비서울_시군구업종별.xlsx
  06_NICE_대출연체_시군구별.xlsx
  07_NICE_소득_시군구별.xlsx
  08_아파트단지소비_시군구별.xlsx
  09_농협카드_아파트단지소비_시군구별.xlsx
  10_외식업물가_시군구별.xlsx

output/images/
  01_유형별_분포.png             ← 수치 라벨 없음 (규정 3)
  02_자산소득_산점도.png         ← 축 눈금 수치 없음
  03_B유형_추이.png              ← y축 수치 없음
```

---

## 7. 메모리 최적화 전략

```python
CHUNK = 200_000  # 청크 크기 (VDR 환경)

# dtype 사전 지정 — 메모리 ~40% 절감
dtype = {'CRTR_YR': 'category', 'ADMDST_CLSF_CD': 'category', ...}

# 월별 파일 순차 처리 패턴
def iter_monthly_files(directory, prefix, suffix='.txt', months=range(1,13)):
    for m in months:
        fname = prefix.replace('01', f'{m:02d}') + suffix
        p = directory / fname
        if p.exists():
            yield m, p

# 청크 처리 후 즉시 집계 → 원본 삭제
for chunk in pd.read_csv(path, chunksize=CHUNK):
    agg = chunk.groupby(...).agg(...).reset_index()
    results.append(agg)
    del chunk
gc.collect()
```

---

## 8. 팀원 간 산출물 인터페이스

### 팀원 A → 팀원 B (모델링용)
```python
# for_teamB_model_input.csv 컬럼
['가구ID', '연령그룹', '총소득', '순자산', '부동산편중도',
 'D_Value', 'LTI', '연체여부', '이상치_플래그', '페르소나_후보',
 '순자산_분위', '소득_분위']
```

### 팀원 A → 팀원 C (공간분석용)
```python
# for_teamC_sgis_grid.csv 컬럼 (격자 단위 집계)
['격자ID', '고령가구수', '평균LTI', '평균AAI',
 '유동성함정가구수', '유동성함정비율', '평균D_Value']
```

---

## 9. 코드 파일 구조

```
vdr_pipeline.py          ← VDR 반출 규정 준수 메인 파이프라인 (실행 파일)
master_table_design.py   ← Master Table 스키마 정의 (77개 컬럼)
week1_data_pipeline.py   ← 1~2주차: 데이터 결합·정제
week3_4_feature_engineering.py  ← 3~4주차: 파생변수·연관규칙
week5_6_validation_eda.py       ← 5~6주차: 검증·EDA·명세서

sample_data/             ← 테스트용 샘플 데이터 (VDR 경로 구조 동일)
```

### 실행 순서
```python
# VDR 환경에서 경로 2줄만 수정 후 실행
DRIVE = Path('/content/drive/MyDrive/Data_Analysis_Competition')
RDATA = Path('/Rdata1/r1_user138/dataset')

# 실행
python vdr_pipeline.py
```

---

## 10. 수상 포인트 (팀원 A 기여 핵심)

1. **D-Value**: 소득통계상 빈곤 ≠ 실제 소비 → 통계의 한계를 데이터로 직접 증명
2. **LTI**: 자산·연체·소득·금융자산 4가지 결합 → 유동성 위기 가구 정량화
3. **이상치 처리**: "소득=0 + 부동산 보유" 가구를 삭제하지 않고 `유지_핵심대상` 플래그 → B유형 발굴의 근거
4. **3중 교차 검증**: 정부기준빈곤선 + 상대소득분위 + 상대자산분위 → "20만 가구"의 통계적 명확성
5. **Master Table**: 팀 전체가 동일 데이터 기반 → 1주차 EDA 스토리가 6주차 정책 제언까지 일관성 유지

---

## 11. 자주 발생하는 에러 및 해결법

| 에러 | 원인 | 해결 |
|------|------|------|
| `KeyError: '14'` | 연령코드가 정수로 로드됨 | `dtype={'통합카드5세단위연령코드': str}` 명시 |
| `KeyError: '월 연체보유자수 합계'` | 공백 포함 항목명 오타 | 복사-붙여넣기로만 사용 |
| `ZeroDivisionError / inf` | 소득 0 나눗셈 | `+ 1e-6` 또는 `replace([np.inf,-np.inf], np.nan)` |
| `MemoryError` | 대용량 txt 전체 로드 | `chunksize=200_000` + `gc.collect()` |
| `K-Anonymity 전체 삭제` | 집계 단위가 너무 세밀함 | 시군구 → 시도 단위로 상향 |
| 컬럼 수 불일치 (30열 아님) | 가구마스터 헤더 있음 | `skiprows=1` 또는 `header=0` 확인 |

---

## 12. 참고 문서

- 연구보고서 제2534호: 고령인구 자산 분포와 불평등 구조의 변화
- 2026년 국가데이터 활용대회 안내문
- 보건복지부 2026 기준 중위소득 고시
- SGIS 소지역통계 인구·가구·주택 통계등록부 항목명 XLS
- 내국인 국내카드소비(서울) 이용자 설명자료
- NICE 신용통계정보(대출·연체 / 소득) 이용자 설명자료
